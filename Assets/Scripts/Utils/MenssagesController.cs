using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MenssagesController
{
    // levanta el evento para llamar al deseralizado correcto

    // (Deberia ir en NetworkManager)

    // Deberia discriminar entre servidor y cliente (Un if) para broadcastear 

    // tambien ejecua en orden los datos ordenables que le llegan

    // Debe ordenar y ejecutar en orden los mensajes

    // Debe calcular y descartar los mensajes corruptos
}
